# welcomepng — post-install page for HTML to PNG

Hosted on GitHub Pages: https://alkhimik888.github.io/welcomepng/
Opened by the extension once, right after install (`chrome.runtime.onInstalled`).

## Yandex.Metrika — CONNECTED

Counter **112031289** («HTML to PNG — Welcome», owner's account) is wired into
`index.html` (2026-08-28). One counter per welcome page: welcomepng uses its own
(112031184 — welcomeqr). Installs are visible as visits to this page (each install opens it
exactly once). The privacy policy for the extension lives at `/policy/`.
